// Minimal bootstrap file to satisfy `import './bootstrap'` in app.js
// Add any global JS setup (axios, Echo, polyfills) here if needed.

// Intentionally minimal so Vite can resolve the import even if no deps.
export default {};
